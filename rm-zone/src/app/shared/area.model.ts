export class Area {

    constructor(
        public id: number, 
        public name: string, 
        public district: string, 
        public zone: string, 
        public missionId: number
    ) {}
}